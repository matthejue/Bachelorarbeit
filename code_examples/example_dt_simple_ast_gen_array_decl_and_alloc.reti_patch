File 
  Name './example_dt_simple_ast_gen_array_decl_and_alloc.reti_patch', 
  [
    Block 
      Name 'start.1', 
      [
        # // Exp(GoTo(Name('main.0')))
        # // patched out Exp(GoTo(Name('main.0')))
      ], 
    Block 
      Name 'main.0', 
      [
        # // Exp(Alloc(Writeable(), PntrDecl(Num('1'), ArrayDecl([Num('3'), Num('2')], PntrDecl(Num('1'), StructSpec(Name('st'))))), Name('var')))
        # Return(Empty())
        LOADIN BAF PC -1;
      ]
  ]